/node_modules
/.cache
package-lock.json
dist
.env
.env.local
.vercel
.env*

# Reference photos/screenshots dropped at the project root for review — never tracked.
# (public/ and docs/ images are unaffected: the leading slash anchors these to root only.)
/*.jpg
/*.jpeg
/*.png
/*.heic
/*.webp
