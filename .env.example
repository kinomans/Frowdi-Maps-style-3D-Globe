# Free Cesium ion account & token: https://ion.cesium.com/tokens
# Needed for real, non-rate-limited World Terrain + OSM Buildings (clickable buildings).
# The app still runs without it, using Cesium's shared demo token (rate-limited, dev only).
VITE_CESIUM_ION_TOKEN=
